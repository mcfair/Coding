farthest reach so far

public boolean canReachEnd(List<Integer> maxAdvanceSteps) {
	int furthest = 0;
	last = maxAdvanceSteps.size() - 1;
	for (int i = 0; i <= furthest; i ++) {
		furthest = Math.max(i + maxAdvanceSteps.get(i), furthest);
	}
	return furthest >= last
}